# Django
Django>=4.2,<5.0

# Database
# SQLite is included with Python, no additional package needed

# Email (for OTP verification)
# Django's built-in email backend is used, no additional packages needed for basic SMTP

# Static files
# Django's built-in static files handling is used

# Development
# No additional development packages required for basic setup

# Optional: For production deployment
# gunicorn>=21.0.0
# whitenoise>=6.0.0
# psycopg2-binary>=2.9.0  # For PostgreSQL support
# django-environ>=0.11.0  # For environment variable management
